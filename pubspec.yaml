name: skype_clone_flutter_tdd_clean_architecture
description: A new Flutter project.


publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  equatable: ^2.0.0
  dartz: ^0.9.2
  firebase_core: ^1.0.1
  firebase_auth: ^1.0.1
  google_sign_in: ^5.0.0
  flutter_bloc: ^6.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^4.1.2
  bloc_test: ^7.1.0

flutter:
  uses-material-design: true

  